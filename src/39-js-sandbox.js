/* Sandbox do runner JavaScript.

   Duas partes:
     · WORKER_SOURCE — o script, como STRING, que roda dentro do Worker. A mesma
       fonte é usada em produção (Blob -> Worker dentro de um iframe sandbox) e nos
       testes (executada em um contexto isolado do Node). Executar código do aluno
       aqui é seguro porque isto NÃO é o realm da aplicação: em produção é um Worker
       dentro de um iframe `sandbox="allow-scripts"` sem `allow-same-origin`, de
       origem opaca; no Node é um contexto `vm` sem acesso ao LX.
     · createBrowserTransport — monta essa fronteira no navegador e fala com o
       coordenador (39-js-runner.js) pelo contrato open/post/terminate.

   O código dentro de WORKER_SOURCE é propositalmente autocontido: ele não pode
   importar nada do LX (esse é justamente o ponto do isolamento). A inspeção de
   console é uma versão compacta da que existe no protocolo. */
'use strict';
(function () {
  const JS = (LX.JS = LX.JS || {});

  const WORKER_SOURCE = [
    "'use strict';",
    "(function () {",
    "  var post = (self && self.postMessage) ? self.postMessage.bind(self) : function () {};",
    "  var LIMITS = { OUTPUT_BYTES: 262144, STRING_LEN: 10000, INSPECT_DEPTH: 6, INSPECT_ITEMS: 100 };",
    "  var files = {};",
    "  var G = (typeof globalThis !== 'undefined') ? globalThis : self;",
    "  var nSetTimeout = (self && self.setTimeout) ? self.setTimeout.bind(self) : (typeof setTimeout !== 'undefined' ? setTimeout : null);",
    "  var nClearTimeout = (self && self.clearTimeout) ? self.clearTimeout.bind(self) : (typeof clearTimeout !== 'undefined' ? clearTimeout : null);",
    "  var nSetInterval = (self && self.setInterval) ? self.setInterval.bind(self) : (typeof setInterval !== 'undefined' ? setInterval : null);",
    "  var nClearInterval = (self && self.clearInterval) ? self.clearInterval.bind(self) : (typeof clearInterval !== 'undefined' ? clearInterval : null);",
    "  var nQueueMicro = (typeof queueMicrotask !== 'undefined') ? queueMicrotask : function (fn) { Promise.resolve().then(fn); };",
    "  // Estado do event loop da execução atual (uma execução por vez).",
    "  var curRun = null, curEntry = null, hadError = false, timersLeft = 0, timerIds = {}, intervalIds = {}, tests = [], describeStack = [];",
    "  var capSeq = 0, capPending = {};",
    "  function countPending() { var n = 0; for (var k in capPending) if (Object.prototype.hasOwnProperty.call(capPending, k)) n++; return n; }",
    "  function capRequest(cap, method, args) {",
    "    return new Promise(function (resolve, reject) {",
    "      if (!curRun) { reject(new Error('Sem execução ativa.')); return; }",
    "      var id = 'c' + (++capSeq);",
    "      capPending[id] = { resolve: resolve, reject: reject };",
    "      post({ type: 'cap', runId: curRun, id: id, cap: cap, method: method, args: args || [] });",
    "    });",
    "  }",
    "  function capError(e) { var err = new Error(String(e && e.message != null ? e.message : 'Erro na capability')); err.name = (e && e.name) || 'CapabilityError'; return err; }",
    "  var PARAMS = 'console, self, postMessage, onmessage, globalThis, importScripts, close, setTimeout, setInterval, clearTimeout, clearInterval, queueMicrotask, describe, it, test, expect, beforeEach, afterEach, process, Buffer, require';",
    "  var PROLOGUE = '\"use strict\";\\n';",
    "  var OFFSET = null;",
    "  function wrapperOffset() {",
    "    if (OFFSET !== null) return OFFSET;",
    "    OFFSET = 0;",
    "    try { new Function(PARAMS, PROLOGUE + 'throw new Error(\"p\");\\n//# sourceURL=__lx_probe__')(); }",
    "    catch (e) { if (e && e.stack) { var m = String(e.stack).match(/__lx_probe__:(\\d+):/); if (m) OFFSET = Number(m[1]) - 1; } }",
    "    return OFFSET;",
    "  }",
    "  function byteLen(t) { try { return new TextEncoder().encode(t).length; } catch (e) { return t.length; } }",
    "  function clip(t) { return t.length > LIMITS.STRING_LEN ? t.slice(0, LIMITS.STRING_LEN) + '\\u2026 (truncado)' : t; }",
    "  function inspect(value) {",
    "    var seen = [];",
    "    function walk(node, depth) {",
    "      var type = typeof node;",
    "      if (node === null) return 'null';",
    "      if (type === 'undefined') return 'undefined';",
    "      if (type === 'string') return depth === 0 ? clip(node) : JSON.stringify(clip(node));",
    "      if (type === 'number' || type === 'boolean') return String(node);",
    "      if (type === 'bigint') return String(node) + 'n';",
    "      if (type === 'symbol') return node.toString();",
    "      if (type === 'function') return '[Function: ' + (node.name || 'anonima') + ']';",
    "      if (node instanceof Error) return node.name + ': ' + clip(String(node.message));",
    "      if (depth >= LIMITS.INSPECT_DEPTH) return Array.isArray(node) ? '[Array]' : '[Object]';",
    "      if (seen.indexOf(node) !== -1) return '[Circular]';",
    "      seen.push(node);",
    "      try {",
    "        if (Array.isArray(node)) {",
    "          var parts = [];",
    "          for (var i = 0; i < node.length && i < LIMITS.INSPECT_ITEMS; i++) parts.push(walk(node[i], depth + 1));",
    "          if (node.length > LIMITS.INSPECT_ITEMS) parts.push('\\u2026 +' + (node.length - LIMITS.INSPECT_ITEMS) + ' itens');",
    "          return '[' + parts.join(', ') + ']';",
    "        }",
    "        var keys = Object.keys(node), out = [];",
    "        for (var k = 0; k < keys.length && k < LIMITS.INSPECT_ITEMS; k++) {",
    "          var label = /^[A-Za-z_$][\\w$]*$/.test(keys[k]) ? keys[k] : JSON.stringify(keys[k]);",
    "          out.push(label + ': ' + walk(node[keys[k]], depth + 1));",
    "        }",
    "        if (keys.length > LIMITS.INSPECT_ITEMS) out.push('\\u2026 +' + (keys.length - LIMITS.INSPECT_ITEMS) + ' chaves');",
    "        var ctor = node.constructor && node.constructor.name;",
    "        var prefix = ctor && ctor !== 'Object' ? ctor + ' ' : '';",
    "        return prefix + '{' + out.join(', ') + '}';",
    "      } finally { seen.pop(); }",
    "    }",
    "    try { return walk(value, 0); } catch (e) { return '[valor nao inspecionavel]'; }",
    "  }",
    "  function formatArgs(args) {",
    "    var parts = [];",
    "    for (var i = 0; i < args.length; i++) parts.push(inspect(args[i]));",
    "    return parts.join(' ');",
    "  }",
    "  function makeConsole(runId, state) {",
    "    function sink(level) {",
    "      return function () {",
    "        if (state.stopped) return;",
    "        var text = formatArgs(Array.prototype.slice.call(arguments));",
    "        state.bytes += byteLen(text);",
    "        if (state.bytes > LIMITS.OUTPUT_BYTES) { state.stopped = true; post({ type: 'output-limit', runId: runId, kind: 'console' }); return; }",
    "        post({ type: 'console', runId: runId, level: level, text: text });",
    "      };",
    "    }",
    "    return { log: sink('log'), info: sink('info'), warn: sink('warn'), error: sink('error'), debug: sink('debug') };",
    "  }",
    "  function locate(error, entry) {",
    "    var info = { name: 'Error', message: String(error), stack: null, line: null, column: null, file: entry };",
    "    if (error && typeof error === 'object') {",
    "      info.name = String(error.name || 'Error');",
    "      info.message = String(error.message == null ? error : error.message);",
    "      if (error.stack) info.stack = String(error.stack);",
    "      if (typeof error.lineNumber === 'number') info.line = error.lineNumber - wrapperOffset();",
    "      if (typeof error.columnNumber === 'number') info.column = error.columnNumber;",
    "    }",
    "    if ((info.line == null) && info.stack) {",
    "      var re = new RegExp(entry.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + ':(\\\\d+):(\\\\d+)');",
    "      var m = info.stack.match(re);",
    "      if (m) { info.line = Number(m[1]) - wrapperOffset(); info.column = Number(m[2]); }",
    "    }",
    "    if (info.line != null && !(info.line > 0)) info.line = null;",
    "    return info;",
    "  }",
    "  function reportError(err) { hadError = true; if (curRun) post({ type: 'uncaught-error', runId: curRun, error: locate(err, curEntry) }); }",
    "  function wrapTimeout(fn, ms) {",
    "    if (typeof fn !== 'function' || !nSetTimeout) return nSetTimeout ? nSetTimeout(fn, ms) : 0;",
    "    var extra = Array.prototype.slice.call(arguments, 2);",
    "    timersLeft++;",
    "    var id = nSetTimeout(function () { if (timerIds[id]) { delete timerIds[id]; timersLeft--; } try { fn.apply(undefined, extra); } catch (e) { reportError(e); } }, ms);",
    "    timerIds[id] = true; return id;",
    "  }",
    "  function wrapClearTimeout(id) { if (timerIds[id]) { delete timerIds[id]; timersLeft--; } if (nClearTimeout) nClearTimeout(id); }",
    "  function wrapInterval(fn, ms) {",
    "    if (typeof fn !== 'function' || !nSetInterval) return nSetInterval ? nSetInterval(fn, ms) : 0;",
    "    var extra = Array.prototype.slice.call(arguments, 2);",
    "    timersLeft++;",
    "    var id = nSetInterval(function () { try { fn.apply(undefined, extra); } catch (e) { reportError(e); } }, ms);",
    "    intervalIds[id] = true; return id;",
    "  }",
    "  function wrapClearInterval(id) { if (intervalIds[id]) { delete intervalIds[id]; timersLeft--; if (nClearInterval) nClearInterval(id); } }",
    "  // Espera o event loop esvaziar: microtasks drenam entre macrotasks, então",
    "  // dois giros seguidos sem timers pendentes significam ocioso. O tempo limite",
    "  // do coordenador encerra o Worker se isto nunca terminar.",
    "  function drainUntilIdle() {",
    "    return new Promise(function (resolve) {",
    "      if (!nSetTimeout) { resolve(); return; }",
    "      var quiet = 0;",
    "      (function tick() {",
    "        if (timersLeft > 0 || countPending() > 0) { quiet = 0; nSetTimeout(tick, 0); return; }",
    "        quiet++;",
    "        if (quiet >= 2) resolve(); else nSetTimeout(tick, 0);",
    "      })();",
    "    });",
    "  }",
    "  // ---- test runner didático (describe/it/expect, hooks e async) ----",
    "  function inspectSafe(v) { try { return inspect(v); } catch (e) { return String(v); } }",
    "  function makeExpect(value) {",
    "    function fail(msg) { var e = new Error(msg); e.name = 'AssertionError'; throw e; }",
    "    function eq(a, b) { return a === b || (a !== a && b !== b); }",
    "    function deep(a, b) { try { return JSON.stringify(a) === JSON.stringify(b); } catch (e) { return a === b; } }",
    "    var api = {",
    "      toBe: function (exp) { if (!eq(value, exp)) fail('esperava ' + inspectSafe(exp) + ', recebeu ' + inspectSafe(value)); },",
    "      toEqual: function (exp) { if (!deep(value, exp)) fail('esperava (igual) ' + inspectSafe(exp) + ', recebeu ' + inspectSafe(value)); },",
    "      toBeTruthy: function () { if (!value) fail('esperava verdadeiro, recebeu ' + inspectSafe(value)); },",
    "      toBeFalsy: function () { if (value) fail('esperava falso, recebeu ' + inspectSafe(value)); },",
    "      toBeCloseTo: function (exp, d) { var p = Math.pow(10, -(d == null ? 2 : d)) / 2; if (!(Math.abs(value - exp) <= p)) fail('esperava perto de ' + exp + ', recebeu ' + value); },",
    "      toThrow: function () { var t = false; try { if (typeof value === 'function') value(); } catch (e) { t = true; } if (!t) fail('esperava que lançasse um erro'); },",
    "      toContain: function (item) { var ok = value && value.indexOf && value.indexOf(item) !== -1; if (!ok) fail('esperava conter ' + inspectSafe(item)); }",
    "    };",
    "    api.not = {",
    "      toBe: function (exp) { if (eq(value, exp)) fail('esperava diferente de ' + inspectSafe(exp)); },",
    "      toEqual: function (exp) { if (deep(value, exp)) fail('esperava diferente de ' + inspectSafe(exp)); }",
    "    };",
    "    return api;",
    "  }",
    "  function describeFn(name, fn) { describeStack.push({ name: String(name), before: [], after: [] }); try { if (typeof fn === 'function') fn(); } finally { describeStack.pop(); } }",
    "  function itFn(name, fn) { tests.push({ name: String(name), fn: fn, path: describeStack.map(function (d) { return d.name; }).filter(Boolean), beforeRefs: describeStack.map(function (d) { return d.before; }), afterRefs: describeStack.map(function (d) { return d.after; }) }); }",
    "  function beforeEachFn(fn) { if (typeof fn === 'function' && describeStack.length) describeStack[describeStack.length - 1].before.push(fn); }",
    "  function afterEachFn(fn) { if (typeof fn === 'function' && describeStack.length) describeStack[describeStack.length - 1].after.push(fn); }",
    "  function fullName(t) { return t.path.concat([t.name]).join(' \\u203a '); }",
    "  function runTestsIfAny(runId) {",
    "    if (!tests.length) return Promise.resolve();",
    "    var list = tests.slice(); var passed = 0, failed = 0;",
    "    function runOne(i) {",
    "      if (i >= list.length) { post({ type: 'diagnostic', runId: runId, payload: { kind: 'test-summary', total: list.length, passed: passed, failed: failed } }); if (failed) hadError = true; return Promise.resolve(); }",
    "      var t = list[i], name = fullName(t), t0 = Date.now();",
    "      post({ type: 'test-start', runId: runId, name: name });",
    "      var chain = Promise.resolve();",
    "      t.beforeRefs.forEach(function (refs) { refs.forEach(function (h) { chain = chain.then(function () { return h(); }); }); });",
    "      chain = chain.then(function () { return t.fn && t.fn(); });",
    "      t.afterRefs.slice().reverse().forEach(function (refs) { refs.slice().reverse().forEach(function (h) { chain = chain.then(function () { return h(); }); }); });",
    "      return chain.then(function () { passed++; post({ type: 'test-result', runId: runId, name: name, ok: true, durationMs: Date.now() - t0 }); }, function (err) { failed++; post({ type: 'test-result', runId: runId, name: name, ok: false, message: String(err && err.message != null ? err.message : err), durationMs: Date.now() - t0 }); }).then(function () { return runOne(i + 1); });",
    "    }",
    "    return runOne(0);",
    "  }",
    "  // ---- módulos embutidos didáticos (Node): process, path, events, Buffer ----",
    "  var nodeProcess = { argv: ['node', 'programa.js'], env: {}, platform: 'browser', arch: 'wasm', pid: 1, version: 'terminalis', versions: {}, cwd: function () { return '/'; }, nextTick: function (fn) { var a = Array.prototype.slice.call(arguments, 1); nQueueMicro(function () { if (typeof fn === 'function') fn.apply(undefined, a); }); }, exit: function () {}, on: function () {}, once: function () {} };",
    "  var nodePath = (function () {",
    "    function normArr(parts, allowAbove) { var up = 0, res = []; for (var i = parts.length - 1; i >= 0; i--) { var p = parts[i]; if (!p || p === '.') continue; else if (p === '..') up++; else if (up) up--; else res.unshift(p); } if (allowAbove) while (up-- > 0) res.unshift('..'); return res; }",
    "    function normalize(path) { path = String(path); var abs = path.charAt(0) === '/'; var trail = path.charAt(path.length - 1) === '/'; var segs = normArr(path.split('/'), !abs).join('/'); if (!segs && !abs) segs = '.'; if (segs && trail) segs += '/'; return (abs ? '/' : '') + segs; }",
    "    function join() { var parts = Array.prototype.slice.call(arguments).filter(function (p) { return typeof p === 'string' && p; }); return parts.length ? normalize(parts.join('/')) : '.'; }",
    "    function dirname(path) { var n = normalize(path).replace(/\\/$/, ''); var i = n.lastIndexOf('/'); return i < 0 ? '.' : (i === 0 ? '/' : n.slice(0, i)); }",
    "    function basename(path, ext) { var n = normalize(path).replace(/\\/$/, ''); var b = n.slice(n.lastIndexOf('/') + 1); if (ext && b.length > ext.length && b.slice(-ext.length) === ext) b = b.slice(0, -ext.length); return b; }",
    "    function extname(path) { var b = basename(path); var i = b.lastIndexOf('.'); return i > 0 ? b.slice(i) : ''; }",
    "    return { sep: '/', delimiter: ':', normalize: normalize, join: join, dirname: dirname, basename: basename, extname: extname, isAbsolute: function (p) { return typeof p === 'string' && p.charAt(0) === '/'; } };",
    "  })();",
    "  function NodeEventEmitter() { this._ev = {}; }",
    "  NodeEventEmitter.prototype.on = function (name, fn) { (this._ev[name] = this._ev[name] || []).push(fn); return this; };",
    "  NodeEventEmitter.prototype.addListener = NodeEventEmitter.prototype.on;",
    "  NodeEventEmitter.prototype.once = function (name, fn) { var self = this; function g() { self.off(name, g); return fn.apply(this, arguments); } g._orig = fn; return this.on(name, g); };",
    "  NodeEventEmitter.prototype.off = function (name, fn) { var l = this._ev[name]; if (l) this._ev[name] = l.filter(function (f) { return f !== fn && f._orig !== fn; }); return this; };",
    "  NodeEventEmitter.prototype.removeListener = NodeEventEmitter.prototype.off;",
    "  NodeEventEmitter.prototype.removeAllListeners = function (name) { if (name) delete this._ev[name]; else this._ev = {}; return this; };",
    "  NodeEventEmitter.prototype.emit = function (name) { var l = (this._ev[name] || []).slice(); var a = Array.prototype.slice.call(arguments, 1); for (var i = 0; i < l.length; i++) l[i].apply(this, a); return l.length > 0; };",
    "  NodeEventEmitter.prototype.listenerCount = function (name) { return (this._ev[name] || []).length; };",
    "  var nodeBuffer = (function () {",
    "    if (typeof Buffer !== 'undefined') return Buffer;",
    "    var enc = (typeof TextEncoder !== 'undefined') ? new TextEncoder() : null;",
    "    var dec = (typeof TextDecoder !== 'undefined') ? new TextDecoder() : null;",
    "    var B64 = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';",
    "    function toBase64(u) { var out = ''; for (var i = 0; i < u.length; i += 3) { var a = u[i], b = i + 1 < u.length ? u[i + 1] : 0, c = i + 2 < u.length ? u[i + 2] : 0; out += B64[a >> 2] + B64[((a & 3) << 4) | (b >> 4)] + (i + 1 < u.length ? B64[((b & 15) << 2) | (c >> 6)] : '=') + (i + 2 < u.length ? B64[c & 63] : '='); } return out; }",
    "    function wrap(u) { var o = { _u: u, _isBuffer: true, length: u.length }; for (var i = 0; i < u.length; i++) o[i] = u[i]; o.toString = function (e) { if (e === 'hex') { var s = ''; for (var j = 0; j < u.length; j++) s += ('0' + u[j].toString(16)).slice(-2); return s; } if (e === 'base64') return toBase64(u); return dec ? dec.decode(u) : String.fromCharCode.apply(null, Array.prototype.slice.call(u)); }; o.slice = function (a, b) { return wrap(u.slice(a, b)); }; return o; }",
    "    return { from: function (input) { if (typeof input === 'string') return wrap(enc ? enc.encode(input) : new Uint8Array(0)); return wrap(Uint8Array.from(input || [])); }, alloc: function (n, f) { var u = new Uint8Array(n); if (f != null) u.fill(f); return wrap(u); }, isBuffer: function (x) { return !!(x && x._isBuffer); }, concat: function (list) { var t = 0, i; for (i = 0; i < list.length; i++) t += list[i].length; var out = new Uint8Array(t), off = 0; for (i = 0; i < list.length; i++) { out.set(list[i]._u || list[i], off); off += list[i].length; } return wrap(out); } };",
    "  })();",
    "  // fs sobre o VFS por RPC (assíncrono; sync exigiria SharedArrayBuffer).",
    "  function fsCall(method, args) { return capRequest('fs', method, args).then(null, function (e) { throw capError(e); }); }",
    "  var fsPromises = {",
    "    readFile: function (p) { return fsCall('readFile', [String(p)]); },",
    "    writeFile: function (p, d) { return fsCall('writeFile', [String(p), String(d)]); },",
    "    readdir: function (p) { return fsCall('readdir', [String(p)]); },",
    "    mkdir: function (p) { return fsCall('mkdir', [String(p)]); },",
    "    stat: function (p) { return fsCall('stat', [String(p)]); },",
    "    unlink: function (p) { return fsCall('unlink', [String(p)]); }",
    "  };",
    "  function cbFrom(promise, cb) { promise.then(function (v) { if (typeof cb === 'function') cb(null, v); }, function (e) { if (typeof cb === 'function') cb(e); }); }",
    "  var nodeFs = {",
    "    promises: fsPromises,",
    "    readFile: function (p, a, b) { cbFrom(fsPromises.readFile(p), typeof a === 'function' ? a : b); },",
    "    writeFile: function (p, d, a, b) { cbFrom(fsPromises.writeFile(p, d), typeof a === 'function' ? a : b); },",
    "    readdir: function (p, a, b) { cbFrom(fsPromises.readdir(p), typeof a === 'function' ? a : b); }",
    "  };",
    "  function nodeRequire(name) { var n = String(name).replace(/^node:/, ''); if (n === 'events') return { EventEmitter: NodeEventEmitter, default: NodeEventEmitter }; if (n === 'path') return nodePath; if (n === 'process') return nodeProcess; if (n === 'buffer') return { Buffer: nodeBuffer }; if (n === 'fs') return nodeFs; if (n === 'fs/promises') return fsPromises; var e = new Error(\"Módulo '\" + name + \"' não disponível (a sandbox não busca pacotes externos).\"); e.name = 'ModuleError'; throw e; }",
    "  // ---- módulos ESM: grafo de Blobs com imports relativos reescritos ----",
    "  var dynImport = null, dynImportTried = false;",
    "  function getDynImport() {",
    "    if (dynImportTried) return dynImport;",
    "    dynImportTried = true;",
    "    try { dynImport = new Function('u', 'return import(u);'); } catch (e) { dynImport = null; }",
    "    return dynImport;",
    "  }",
    "  function isModuleSource(src) { return /(^|[\\n;])\\s*export\\b/.test(src) || /(^|[\\n;])\\s*import\\s+[\\w{*'\\\"]/.test(src) || /(^|[\\n;])\\s*import\\s*\\{/.test(src); }",
    "  function dirOf(p) { var i = p.lastIndexOf('/'); return i < 0 ? '' : p.slice(0, i); }",
    "  function normalizePath(base, rel) {",
    "    var parts = base ? base.split('/') : [], segs = rel.split('/');",
    "    for (var i = 0; i < segs.length; i++) { var s = segs[i]; if (s === '' || s === '.') continue; if (s === '..') parts.pop(); else parts.push(s); }",
    "    return parts.join('/');",
    "  }",
    "  function resolveSpec(spec, importer) {",
    "    var p = normalizePath(dirOf(importer), spec);",
    "    if (Object.prototype.hasOwnProperty.call(files, p)) return p;",
    "    if (Object.prototype.hasOwnProperty.call(files, p + '.js')) return p + '.js';",
    "    if (Object.prototype.hasOwnProperty.call(files, p + '/index.js')) return p + '/index.js';",
    "    return null;",
    "  }",
    "  function buildModule(path, urls, stack) {",
    "    if (urls[path]) return urls[path];",
    "    if (stack.indexOf(path) !== -1) { var ce = new Error('Ciclo de módulos não suportado: ' + stack.concat([path]).join(' -> ')); ce.name = 'ModuleError'; throw ce; }",
    "    stack.push(path);",
    "    var src = String(files[path]);",
    "    var re = /(?:\\bfrom|\\bimport)\\s*\\(?\\s*(['\\\"])([^'\\\"]+)\\1/g;",
    "    var rewritten = src.replace(re, function (m, q, spec) {",
    "      if (spec.charAt(0) !== '.') { var be = new Error('Import externo não suportado (sem pacotes): ' + spec); be.name = 'ModuleError'; throw be; }",
    "      var resolved = resolveSpec(spec, path);",
    "      if (!resolved) { var re2 = new Error('Módulo não encontrado: ' + spec + ' (em ' + path + ')'); re2.name = 'ResolutionError'; throw re2; }",
    "      var url = buildModule(resolved, urls, stack);",
    "      return m.replace(q + spec + q, q + url + q);",
    "    });",
    "    stack.pop();",
    "    rewritten += '\\n//# sourceURL=' + path;",
    "    var url2 = URL.createObjectURL(new Blob([rewritten], { type: 'text/javascript' }));",
    "    urls[path] = url2;",
    "    if (curRun) post({ type: 'module-loaded', runId: curRun, path: path });",
    "    return url2;",
    "  }",
    "  function revokeUrls(urls) { for (var k in urls) { try { URL.revokeObjectURL(urls[k]); } catch (e) {} } }",
    "  function installGlobals(sandboxConsole) {",
    "    var g = self, keys = ['console', 'setTimeout', 'setInterval', 'clearTimeout', 'clearInterval', 'queueMicrotask', 'describe', 'it', 'test', 'expect', 'beforeEach', 'afterEach', 'process', 'Buffer', 'require'];",
    "    var vals = { console: sandboxConsole, setTimeout: wrapTimeout, setInterval: wrapInterval, clearTimeout: wrapClearTimeout, clearInterval: wrapClearInterval, queueMicrotask: nQueueMicro, describe: describeFn, it: itFn, test: itFn, expect: makeExpect, beforeEach: beforeEachFn, afterEach: afterEachFn, process: nodeProcess, Buffer: nodeBuffer, require: nodeRequire };",
    "    var saved = {}, had = {};",
    "    for (var i = 0; i < keys.length; i++) { var k = keys[i]; had[k] = Object.prototype.hasOwnProperty.call(g, k); saved[k] = g[k]; try { g[k] = vals[k]; } catch (e) {} }",
    "    return function restore() { for (var j = 0; j < keys.length; j++) { var kk = keys[j]; try { if (had[kk]) g[kk] = saved[kk]; else delete g[kk]; } catch (e) {} } };",
    "  }",
    "  function runModule(runId, entry, sandboxConsole) {",
    "    var di = getDynImport();",
    "    if (typeof Blob === 'undefined' || typeof URL === 'undefined' || !URL.createObjectURL || !di) {",
    "      post({ type: 'uncaught-error', runId: runId, error: { name: 'ModuleError', message: 'Módulos ESM precisam do navegador.', file: entry } });",
    "      post({ type: 'run-complete', runId: runId, ok: false }); curRun = null; return;",
    "    }",
    "    var urls = {}, entryUrl;",
    "    try { entryUrl = buildModule(entry, urls, []); }",
    "    catch (e) { post({ type: 'uncaught-error', runId: runId, error: locate(e, entry) }); post({ type: 'run-complete', runId: runId, ok: false }); curRun = null; curEntry = null; revokeUrls(urls); return; }",
    "    var restore = installGlobals(sandboxConsole);",
    "    var done = false;",
    "    function finish() { if (done) return; done = true; restore(); revokeUrls(urls); var run = curRun; curRun = null; curEntry = null; post({ type: 'run-complete', runId: run || runId, ok: !hadError }); }",
    "    Promise.resolve().then(function () { return di(entryUrl); })",
    "      .then(function () { return runTestsIfAny(runId); }, function (err) { reportError(err); })",
    "      .then(function () { return drainUntilIdle(); })",
    "      .then(function () { finish(); }, function () { finish(); });",
    "  }",
    "  function runFile(runId, entry) {",
    "    if (!Object.prototype.hasOwnProperty.call(files, entry)) {",
    "      post({ type: 'uncaught-error', runId: runId, error: { name: 'ResolutionError', message: 'Arquivo nao encontrado: ' + entry, file: entry } });",
    "      post({ type: 'run-complete', runId: runId, ok: false });",
    "      return;",
    "    }",
    "    var state = { bytes: 0, stopped: false };",
    "    var sandboxConsole = makeConsole(runId, state);",
    "    curRun = runId; curEntry = entry; hadError = false; timersLeft = 0; timerIds = {}; intervalIds = {}; capPending = {};",
    "    tests = []; describeStack = [{ name: '', before: [], after: [] }];",
    "    nodeProcess.argv = ['node', entry];",
    "    if (isModuleSource(String(files[entry]))) { runModule(runId, entry, sandboxConsole); return; }",
    "    var source = String(files[entry]) + '\\n//# sourceURL=' + entry;",
    "    var factory;",
    "    try { factory = new Function(PARAMS, PROLOGUE + source); }",
    "    catch (syntaxError) {",
    "      curRun = null; curEntry = null;",
    "      post({ type: 'uncaught-error', runId: runId, error: locate(syntaxError, entry) });",
    "      post({ type: 'run-complete', runId: runId, ok: false });",
    "      return;",
    "    }",
    "    var done = false;",
    "    function finish() { if (done) return; done = true; var run = curRun; curRun = null; curEntry = null; post({ type: 'run-complete', runId: run || runId, ok: !hadError }); }",
    "    Promise.resolve().then(function () {",
    "      return factory(sandboxConsole, undefined, undefined, undefined, undefined, undefined, undefined, wrapTimeout, wrapInterval, wrapClearTimeout, wrapClearInterval, nQueueMicro, describeFn, itFn, itFn, makeExpect, beforeEachFn, afterEachFn, nodeProcess, nodeBuffer, nodeRequire);",
    "    }).then(function () { return runTestsIfAny(runId); }, function (err) { reportError(err); })",
    "      .then(function () { return drainUntilIdle(); })",
    "      .then(function () { finish(); }, function () { finish(); });",
    "  }",
    "  function handle(msg) {",
    "    if (!msg || typeof msg !== 'object') return;",
    "    if (msg.type === 'init') { if (msg.limits && typeof msg.limits === 'object') { for (var k in LIMITS) if (typeof msg.limits[k] === 'number') LIMITS[k] = msg.limits[k]; } return; }",
    "    if (msg.type === 'cap-response') { var pend = capPending[msg.id]; if (pend) { delete capPending[msg.id]; if (msg.ok) pend.resolve(msg.value); else pend.reject(msg.error || { message: 'Capability negada' }); } return; }",
    "    if (msg.type === 'run') { files = (msg.files && typeof msg.files === 'object') ? msg.files : {}; runFile(String(msg.runId), String(msg.entrypoint)); return; }",
    "    if (msg.type === 'dispose') { if (self.close) self.close(); return; }",
    "  }",
    "  if (self.addEventListener) self.addEventListener('message', function (ev) { handle(ev.data); });",
    "  else self.onmessage = function (ev) { handle(ev.data); };",
    "  if (self.addEventListener) self.addEventListener('unhandledrejection', function (ev) {",
    "    if (!curRun) return;",
    "    hadError = true;",
    "    post({ type: 'unhandled-rejection', runId: curRun, reason: locate(ev && ev.reason, curEntry) });",
    "    if (ev && ev.preventDefault) ev.preventDefault();",
    "  });",
    "  post({ type: 'ready' });",
    "})();"
  ].join('\n');

  /* HTML do iframe isolado. Origem opaca (sandbox sem allow-same-origin), CSP que
     só permite workers do próprio documento e nenhuma conexão de rede. O bootstrap
     cria o Worker a partir do Blob de WORKER_SOURCE e faz a ponte entre a janela
     pai e o Worker. */
  function iframeHtml() {
    return [
      '<!doctype html><html><head><meta charset="utf-8">',
      // 'unsafe-eval' é intencional e SÓ existe aqui, dentro da origem opaca do
      // iframe/Worker: é assim que o Worker compila o código do aluno (new
      // Function). Isso nunca alcança o realm principal do app. connect-src 'none'
      // mantém a rede negada; worker-src blob: permite o Worker descartável.
      '<meta http-equiv="Content-Security-Policy" content="default-src \'none\'; script-src \'unsafe-inline\' \'unsafe-eval\' blob:; worker-src blob:; connect-src \'none\'">',
      '</head><body><script>',
      '(function(){',
      '  var src = ', JSON.stringify(WORKER_SOURCE), ';',
      '  var worker = null;',
      '  function boot(){',
      '    var blob = new Blob([src], { type: "text/javascript" });',
      '    worker = new Worker(URL.createObjectURL(blob));',
      '    worker.onmessage = function(ev){ parent.postMessage({ __sandbox: true, data: ev.data }, "*"); };',
      '  }',
      '  window.addEventListener("message", function(ev){',
      '    var msg = ev.data;',
      '    if (!msg || !msg.__sandbox) return;',
      '    if (msg.command === "post" && worker) worker.postMessage(msg.data);',
      '  });',
      '  boot();',
      '  parent.postMessage({ __sandbox: true, data: { type: "iframe-ready" } }, "*");',
      '})();',
      '<\/script></body></html>'
    ].join('');
  }

  /* Transport de navegador. Só toca em document/Worker quando chamado, então o
     carregamento no Node (sem DOM) é seguro. Cada sessão ganha seu próprio iframe;
     terminate() remove o iframe e, com ele, o Worker. */
  function createBrowserTransport(config) {
    if (typeof document === 'undefined' || typeof Worker === 'undefined') {
      throw new Error('createBrowserTransport exige um navegador com Worker.');
    }
    config = config || {};
    const host = config.host || document.body;

    function open(sessionId, onMessage) {
      const iframe = document.createElement('iframe');
      iframe.setAttribute('sandbox', 'allow-scripts');
      iframe.setAttribute('aria-hidden', 'true');
      iframe.style.display = 'none';
      iframe.srcdoc = iframeHtml();
      const queue = [];
      let ready = false;

      function onWindowMessage(ev) {
        if (ev.source !== iframe.contentWindow) return;
        const msg = ev.data;
        if (!msg || !msg.__sandbox) return;
        if (msg.data && msg.data.type === 'iframe-ready') {
          ready = true;
          while (queue.length) iframe.contentWindow.postMessage({ __sandbox: true, command: 'post', data: queue.shift() }, '*');
          return;
        }
        try { onMessage(msg.data); } catch (_) { /* assinante quebrado não derruba o transport */ }
      }
      window.addEventListener('message', onWindowMessage);
      host.appendChild(iframe);

      return {
        post(command) {
          if (!ready) { queue.push(command); return; }
          iframe.contentWindow.postMessage({ __sandbox: true, command: 'post', data: command }, '*');
        },
        terminate() {
          window.removeEventListener('message', onWindowMessage);
          if (iframe.parentNode) iframe.parentNode.removeChild(iframe);
        }
      };
    }
    return { open };
  }

  JS.Sandbox = { WORKER_SOURCE, iframeHtml, createBrowserTransport };
})();
